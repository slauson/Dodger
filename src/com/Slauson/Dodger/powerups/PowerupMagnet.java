package com.slauson.dodger.powerups;

import com.slauson.dodger.objects.StationaryPowerup;

import android.graphics.Bitmap;

public class PowerupMagnet extends StationaryPowerup {
	
	public PowerupMagnet(Bitmap bitmap, int x, int y) {
		super(bitmap, x, y);
	}
}
